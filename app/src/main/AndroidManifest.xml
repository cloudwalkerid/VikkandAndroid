<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.go.bps.mamasa.vikkand">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PilihResponden" />
        <activity
            android:name=".PilihJenisResponden"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PilihSurveiActivity" />
        <activity
            android:name=".PilihKualitas"
            android:label="@string/title_activity_pilih_kualitas" />
        <activity android:name=".PilihSurveiRespondenActivity" />
        <activity android:name=".PilihKualitasResponden"></activity>
    </application>

</manifest>